# Docker Dashboard Environment Variables

# Host URL for the dashboard (used in templates)
HOST_URL=http://localhost:5000

# Disable container stats collection (set to 'true' to disable)
# This can help with performance issues in some Docker environments
DISABLE_STATS=false

# Flask configuration
FLASK_ENV=production
FLASK_DEBUG=false
