# Docker Dashboard Environment Variables

# Host URL for the dashboard (used in templates)
HOST_URL=http://localhost:5000

# Docker Connection Settings
# For local Docker daemon (default)
DOCKER_HOST=unix:///var/run/docker.sock

# For remote Docker daemon over TCP (uncomment and configure)
# DOCKER_HOST=tcp://remote-docker-host:2376
# DOCKER_TLS_VERIFY=1
# DOCKER_CERT_PATH=/path/to/docker/certs

# For remote Docker daemon over SSH (requires docker context)
# DOCKER_HOST=ssh://user@remote-host

# Multiple Docker hosts (comma-separated, for future multi-host support)
# DOCKER_HOSTS=unix:///var/run/docker.sock,tcp://remote1:2376,ssh://user@remote2

# Performance Settings for Better Load Times
# Enable detailed stats collection for containers (default: false for better performance)
ENABLE_STATS=false

# Skip stats collection on initial page load for faster rendering (default: true)
SKIP_INITIAL_STATS=true

# Use lightweight container information formatting for faster initial load (default: true)
FAST_INITIAL_LOAD=true

# Legacy setting (deprecated - use ENABLE_STATS instead)
# DISABLE_STATS=false

# Flask configuration
FLASK_ENV=production
FLASK_DEBUG=false
